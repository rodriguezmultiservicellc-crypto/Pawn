================================================================================
PROJECT: Pawn — Multi-Tenant Pawn / Jewelry / Repair SaaS
Operator: Rodriguez Multi Service LLC (Eddy Rodriguez)
File:    Progress.txt
================================================================================

SEE ALSO (do not duplicate here):
  - Architecture / conventions: CLAUDE.md
  - Source of truth: the code itself

────────────────────────────────────────────────────────────────────────────────
│  RESUME HERE — START EVERY NEW SESSION BY READING THIS BLOCK                │
────────────────────────────────────────────────────────────────────────────────

  Last session:   Session 5 (2026-04-27 ~late) — Phase 3 merge, Phase 2
                  PRINT PDF, Phase 4 RETAIL/POS, DL scanner integration,
                  Vercel project loaded with env. Build green at 34
                  routes. Phase 4 (internal) COMPLETE.
  Context budget: HEAVY — large session covering multiple phases plus
                  parallel-agent dispatches. Save and clear before
                  starting the next session if context is over ~80%.
  Last worked on: DL scanner port-kit integration on /customers/new.
                  Before that: Phase 4 POS merge + 0008 apply + 4
                  type fixes; Phase 2 print PDF merge; Phase 3 merge +
                  0007 apply + 2 type fixes; Vercel env import file
                  generated and deleted.

  ACTIVE FILE(S) — last touched this session:
    --- DL scanner (NEW) ---
    - src/lib/dl-parser.ts                            (NEW — AAMVA PDF417 parser)
    - src/components/customers/DlScanner.tsx          (NEW — button + modal)
    - src/app/(staff)/customers/new/form.tsx          (wired with key-bump remount)

    --- Phase 4 POS (NEW) ---
    - patches/0008-retail-pos.sql                     (applied)
    - src/app/(staff)/pos/                            (full module)
    - src/components/pos/                             (12 components)
    - src/lib/pos/{cart,register}.ts                  (NEW)
    - src/lib/stripe/terminal.ts                      (NEW — stub)
    - src/lib/validations/pos.ts                      (NEW)

    --- Phase 2 PRINT PDF (NEW) ---
    - patches/(no migration needed)
    - src/lib/pdf/PawnTicketPDF.tsx                   (NEW — bilingual layout)
    - src/lib/pdf/render-loan-ticket.tsx              (NEW — server renderer)
    - src/lib/pdf/fonts.ts                            (NEW — Turbopack-safe)
    - src/app/api/print/loan/[id]/route.ts            (NEW — GET handler)

    --- Phase 3 (merged) ---
    - patches/0007-repair-tickets.sql                 (applied)
    - src/app/(staff)/repair/                         (full module)
    - src/components/repair/                          (14 components)
    - src/lib/repair/{workflow,billing}.ts            (NEW)
    - src/lib/validations/repair.ts                   (NEW)

    --- Cross-cutting (modified) ---
    - src/types/database.ts                           (REGEN — 87,736 bytes)
    - src/types/database-aliases.ts                   (extended)
    - src/components/layout/Sidebar.tsx               (/pos + /repair live)
    - src/lib/i18n/{en,es}.ts                         (pawn.print.* +
                                                       repair.* + pos.* +
                                                       dlScanner.*)
    - src/lib/audit.ts                                (POS verbs added)
    - src/app/(staff)/customers/[id]/{page,content}.tsx (repair + sales +
                                                          layaway panels)
    - src/app/(staff)/dashboard/{page,content}.tsx   (repair + retail cards)

  RESUME POINT:
    Diagnose the "new clients won't save" smoke-test bug Eddy reported
    mid-session. He didn't return with the diagnostic data — likely
    cause is the optionalTrimmedString schema in
    src/lib/validations/customer.ts: it runs `z.string().min(1)` AFTER
    the trim preprocess, so empty optional fields fail validation with
    a min(1) error. The form returns fieldErrors but renders them
    inline next to each field, not in the top banner — easy to miss.
    Fix: swap the optional schema to convert empty-after-trim to null
    BEFORE the min(1) check, so empty optional fields parse cleanly.

    THEN finish Phase 0 close-out:
      a. Confirm Vercel deployment status + production URL.
      b. Update NEXT_PUBLIC_APP_URL in .env.local AND in Vercel
         (Production env) to the assigned URL. Redeploy.
      c. Rotate the three pasted secrets (anon JWT, service-role JWT,
         sbp_ access token). Update .env.local + Vercel.
      d. Remove the repo-local git user.name + user.email config
         set by the Session 4 housekeeping agent (CLAUDE.md violation —
         use inline `-c` overrides going forward).

    THEN choose the next workstream:
      (a) Phase 5 — Customer portal /portal/loans + Stripe Payment
          Link payoff (high customer-visible value).
      (b) Phase 6 — Communications (Twilio SMS + WhatsApp + Resend
          per-tenant).
      (c) DL scanner upgrades — dl_raw_payload column for compliance
          audit, block expired licenses at save, front-of-license
          webcam photo capture.
      (d) Phase 7 — Reporting + FL LeadsOnline export.

  OPEN ISSUES / BLOCKERS:
    - [BUG OPEN] /customers/new save fails silently — diagnose first
      next session (see RESUME POINT for hypothesis).
    - Three Phase-0 secrets STILL pasted in chat history awaiting
      rotation (anon JWT, service_role JWT, sbp_ access token).
    - Vercel project EXISTS now and env imported, but: deploy status
      unconfirmed; NEXT_PUBLIC_APP_URL still localhost:3060 in env;
      Stripe Connect not yet onboarded so card-present blocks until
      tenant_billing_settings.stripe_account_id is populated.
    - DATABASE_URL still empty in .env.local (low priority).
    - /magic-link, /set-password auth pages still 404 (deferred from
      Phase 0; stub before customer-portal invites go out — Phase 5
      blocker).
    - Pre-push hooks intentionally declined (memory:
      feedback_no_prepush_hooks.md). Don't re-pitch.
    - PAWN PRINT PDF LEGAL DISCLOSURE is a placeholder; needs counsel
      vetting before any live FL pawn intake. Lives in
      pawn.print.legal.placeholder + pawn.print.legal.terms i18n keys.
    - REPO-LOCAL GIT CONFIG. Session 4 housekeeping agent set
      user.name + user.email via `git config --local` because the
      sandbox blocked inline `-c` overrides. CLAUDE.md global rule
      forbids `git config` writes. Decide: keep (one-time setup, no
      future surprises) or revert (`git config --local --unset
      user.email && --unset user.name`). If reverted, future commits
      still need the `-c` flags.
    - DL SCANNER COMPLIANCE PIECES DEFERRED: customers.dl_raw_payload
      TEXT column not yet added (form already POSTs it as a hidden
      field — wire when column lands), block-expired-licenses guard
      in createCustomerAction, front-of-license webcam photo capture.

  KEY DECISIONS LOCKED THIS SESSION:
    - PARALLEL-AGENT WORKTREE PATTERN, FINAL FORM. Session 4 saw
      agents leak into the primary tree. Session 5: agents stayed in
      their worktrees, no leak. The pattern is now: dispatch with
      worktree isolation, agent commits on its branch (or doesn't —
      operator commits at merge time), `git merge --no-ff` from
      primary tree. i18n + Sidebar conflicts merge cleanly because
      each agent appends to a unique namespace / nav entry.
    - DOUBLE-FORCE WORKTREE REMOVAL. The Session 4 sandbox-blocked
      cleanup was a permission issue. `git worktree remove -f -f
      <path>` (note: TWO -f) overrides agent locks. Combined with
      `git branch -D worktree-agent-...` to delete the pinned branch.
      Use this for stale-worktree cleanup going forward.
    - VERCEL ENV IMPORT EXCLUDES DEV-ONLY VARS. SUPABASE_ACCESS_TOKEN
      and SUPABASE_PROJECT_ID are CLI-only (used by db:types and
      apply-migration.mjs from a dev box, not by the runtime app).
      Vercel runtime gets only the 12 keys it needs. Document this
      list in CLAUDE.md eventually.
    - STRIPE TERMINAL STUB. Card-present PaymentIntent creation runs
      server-side against the per-tenant Connect account
      (tenant_billing_settings.stripe_account_id required). Reader
      pairing UI (`@stripe/terminal-js`) deferred until hardware is
      on hand. AddPaymentDialog has a yellow stub-banner + reader_id
      free-text input + a TEST "Mark succeeded" shortcut for smoke-
      testing without a reader. Real path: webhook from Stripe flips
      sale_payments.card_present_status='succeeded'.
    - POS SALE + LAYAWAY 1:1. Every layaway has a parent sales row
      (sale_kind='layaway'). Layaway payments mirror onto
      sale_payments so sales.paid_total stays in sync without a
      recompute pass. Cancellation refund written as a negative-amount
      layaway_payment (CHECK constraint is `amount <> 0`, not
      `amount > 0`).
    - POS LAYAWAY LIFECYCLE. Inventory items: available → held at
      layaway creation, held → sold on balance-zero completion, held
      → available on cancel-with-restock. Mirrors the POS sale
      flow's available → sold transition but with the held
      checkpoint in the middle.
    - BILINGUAL PAWN TICKET PDF LAYOUT. Stacked-pair labels EN/ES
      inline for field rows; full bilingual two-column legal
      disclosure block. JetBrains Mono for tabular numerics; Rausch
      accent on ticket number; Inter for body. Florida pawn statute
      placeholder lives in i18n keys so updates are translation
      patches, not component rewrites. @react-pdf/renderer +
      @fontsource/inter + @fontsource/jetbrains-mono added to
      package.json. Fonts loaded via fs.readFileSync + base64
      data-URL (Turbopack-safe; avoids static-analysis traps).
    - PRINT-LOCK + REPRINT FLOW. Print button on /pawn/[id]: first
      click fires printTicketAction (locks core fields per the
      database trigger from 0005), then opens /api/print/loan/[id]
      in a new tab. Subsequent clicks open the URL directly without
      re-firing the lock action. Button label flips to "Reprint"
      when loan.is_printed=true.
    - DL SCANNER KEY-BUMP REMOUNT PATTERN. CustomerFormFields uses
      uncontrolled defaultValue inputs. To push parsed AAMVA values
      into the DOM without a controlled-component refactor: keep a
      `[initial, setInitial]` state at the form parent, pass it as
      `initial={initial}` and bump a `key={scanCount}` on success.
      React remounts the field tree with new defaults; user-typed
      values before the scan are wiped (acceptable — scanner is
      open-form-and-immediately-scan workflow). Raw AAMVA payload
      passed to onResult callback, carried in a hidden
      dl_raw_payload input for the future audit-column upgrade.
    - DL SCANNER ICON + PALETTE PORT. Kit shipped lucide-react +
      generic Tailwind blues; project uses @phosphor-icons/react +
      project palette (rausch / ink / canvas / hairline / ash /
      error). Swapped on import. Mount detection dropped in favor
      of `typeof document === 'undefined'` guard (avoids the
      react-hooks/set-state-in-effect rule that bit us in the
      RecordPaymentDialog refactor).

  KEY DECISIONS CARRIED FROM PRIOR SESSIONS (still authoritative):
    - Multi-store from day 1 (tenants.parent_tenant_id + tenant_type).
    - Per-tenant module gates: has_pawn / has_repair / has_retail.
    - Stone setting = service_type ENUM on repair tickets.
    - FL LeadsOnline only for v1.
    - Stripe: Connect + Terminal + Payment Links from one account.
    - compliance_log is write-once. Police-report exporter reads from
      it (not loans/sales).
    - Pawn ticket immutability after print (status='extended' is the
      one allowed post-print mutation).
    - Dedicated cloud projects (Rule 17): own GitHub + Supabase + Vercel.
    - NEXT_PUBLIC_APP_URL is the canonical origin source.
    - window.location.assign over router.push for cookie-set navs.
    - Lookup-by-email pattern for inserting profiles tied to auth.users.
    - DUAL SKU FIELDS (sku TEXT + sku_number BIGINT).
    - PAWN FIELDS GATE BY has_pawn IN UI ONLY (schema columns always exist).
    - AUDIT_LOG INSERTS THROUGH ADMIN CLIENT (no INSERT policy on table).
    - JSON-roundtrip narrowing for Supabase JSONB columns.
    - Photo storage path: tenant_id MUST be folder[0] for RLS to work.
    - 'use server' modules export async functions ONLY — never const
      objects. Inline `{}` at the useActionState call site.
    - Forfeit-to-inventory hook (loans → inventory_items source=
      'pawn_forfeit'); abandoned-to-inventory hook (repair_tickets →
      inventory_items source='abandoned_repair').
    - Decimal-safe math (r4 to 4dp) in lib/pawn/math.ts +
      lib/pos/cart.ts + lib/repair/billing.ts. NO floats.

  IMPORTANT CONSTANTS / PATHS:
    - Project root:   c:\Users\rodri\OneDrive\Documents\Pawn\
    - Dev port:       3060
    - Sister apps:    Luna Azul (:3000) — c:\...\LunaAzul Web Sass\
                      Abacus     (:3030) — C:\...\Abacus\
    - Memory dir:     C:\Users\rodri\.claude\projects\
                      c--Users-rodri-OneDrive-Documents-Pawn\memory\
    - Active-tenant cookie: pawn-active-tenant
    - Storage layout:
        customer-documents/<tenant_id>/<customer_id>/<kind>/<uuid>.<ext>
        inventory-photos/<tenant_id>/<item_id>/<uuid>.<ext>
        repair-photos/<tenant_id>/<ticket_id>/<kind>/<uuid>.<ext>
        Where <kind> = 'id_scan' | 'signature' | 'photo' (customer)
                     = 'intake' | 'in_progress' | 'final' | 'reference' | 'pickup' (repair)
    - Number formats:
        SKU                 'INV-'  + LPAD(seq,6,'0')   (INV-000123)
        Pawn ticket         'PT-'   + LPAD(seq,6,'0')   (PT-000123)
        Repair ticket       'RT-'   + LPAD(seq,6,'0')   (RT-000123)
        Sale                'S-'    + LPAD(seq,6,'0')   (S-000123)
        Return              'R-'    + LPAD(seq,6,'0')   (R-000123)
        Layaway             'L-'    + LPAD(seq,6,'0')   (L-000123)
    - First tenant: Eddy's Jewelers Corp.
        tenant_id    1abc8070-0797-4740-8dea-70cbb16060fe
        license_key  CONSUMED on claim
    - DL scanner kit source:
        C:\Users\rodri\OneDrive\Documents\Claude\code\dl-scanner\
    - Vercel env import file pattern: 12 runtime keys, exclude
      SUPABASE_ACCESS_TOKEN + SUPABASE_PROJECT_ID (CLI-only).

================================================================================
PHASE OVERVIEW
================================================================================

  Phase   Description                                Status      Completed
  ------  -----------------------------------------  ----------  ------------
  0       Foundation skeleton                        IN PROGRESS  —
          (~92% complete: scaffold, schema, RLS,
          three layouts, auth pages, tenant
          provisioning, sidebar. Vercel project
          NOW EXISTS + env imported. Outstanding:
          deploy verification, NEXT_PUBLIC_APP_URL
          to production URL, secret rotation.
          Pre-push hooks DECLINED.)
  1       Customers + Inventory                      COMPLETE    2026-04-26
          (Phase 5 polish: DL scanner shipped on
          /customers/new in Session 5.)
  2       Pawn loans (gated on has_pawn)             COMPLETE    2026-04-27
          (Internal flow Session 4; bilingual EN+ES
          PRINT PDF Session 5. Customer portal
          /portal/loans + pay-by-link still PENDING
          under Phase 5.)
  3       Repair / stone setting                     COMPLETE    2026-04-27
          (Session 4 dispatch landed in Session 5.
          Schema, workflow state machine, /repair
          list + new + [id], 21 server actions,
          14 client components, abandoned→inventory
          hook.)
  4       Retail / POS                               COMPLETE    2026-04-27
          (Session 5 — internal flow. Sales,
          returns, layaways, register sessions,
          Stripe Terminal stub. Receipt PDF and
          hardware reader pairing UI deferred.)
  5       Customer portal (full)                     PENDING     —
          (Includes /portal/loans pay-by-link side
          that the Phase 2 spine + print PDF have
          set up for. Blocked by /magic-link +
          /set-password auth pages still 404.)
  6       Communications                             PENDING     —
  7       Reporting + compliance                     PENDING     —
  8       Polish                                     PENDING     —
          (Note: AUDIT LOG VIEWER originally scoped
          here landed early in Session 4 alongside
          Phase 2 — at /audit, with filters and
          deep-links.)

================================================================================
SESSION HISTORY LOG
================================================================================

--- Session 1 (2026-04-26) — Project planning + skeleton docs ---

  1. ANALYZED LUNA AZUL + ABACUS PROGRESS LOGS. Pulled forward multi-tenant
     architecture, RLS helper pattern, getCtx, guards, dev-watchdog,
     Next 16 proxy.ts, db:types safe wrapper, etc.

  2. PROPOSED 11 DOMAIN MODULES (folded stone setting into repair).

  3. DECISIONS LOCKED — multi-store from day 1, FL police-report only v1,
     Stripe Terminal + Payment Links, pawn module gated, dev port 3060.

  4-7. WROTE INITIAL CLAUDE.md + Progress.txt + DESIGN-airbnb.md, locked
     design tokens, semantic-color extension (Success Green / Warning
     Amber matched to Airbnb's #c13515 saturation), photography guidance
     adapted for pawn/jewelry domain, Cloud-isolation Rule 17.

  8-11. SEMANTIC-COLOR + PHOTOGRAPHY + CLOUD-ISOLATION decisions resolved.

  12-15. SCAFFOLD via npx create-next-app to subdir + mv merge. Watchdog
     cloned to :3060. Design tokens locked in tokens.ts. next.config.ts
     turbopack.root pinned to __dirname.

  16. PATCHES/0001-FOUNDATION.SQL written (~470 lines): 3 enums, 8 tables,
     5 RLS helpers, 3 RPCs, immutability triggers.

  17-23. SUPABASE CLIENT LIBRARY, PROXY.TS, I18N skeleton, AUTH PAGES,
     THREE LAYOUTS, /admin/tenants list+new, placeholder database.ts.

  24-26. SCRIPTS/DB-TYPES.MJS safe wrapper, .env.local.example, BUILD
     VERIFIED GREEN at 11 routes.

  27. CLAUDE.MD CLOBBER + RESTORE — scaffold's mv overwrote with @AGENTS.md
     pointer; restored full + added > pointer to AGENTS.md.

  28. PHASE 0 ~70% COMPLETE; HAND-OFF for cloud setup.

--- Session 2 (2026-04-26 ~16:00) — Cloud wiring + first tenant + bug fixes ---

  29. .ENV.LOCAL POPULATED. SUPABASE_PROJECT_ID derived from URL.

  30. TENANTSWITCHER LIFTED FROM PHASE 1 — three new files (component,
     /api/tenant/switch, layout integration).

  31. .GITIGNORE EXTENDED — .claude/, *.ts.tmp, /.turbo/.

  32. GIT INITIALIZED + INITIAL COMMIT + PUSHED. 63 files, commit 09db205,
     authored as rodriguezmultiservicellc to match Vercel identity.

  33. CLAUDE.MD CLOBBER + RESTORE (again — same scaffold issue).

  34. SCHEMA APPLIED — 0001 ran in Supabase SQL Editor.

  35. SUPERADMIN ONBOARDED — eddydidier@gmail.com via Supabase Auth →
     SELECT-from-auth.users-by-email INSERT pattern (avoids manual UUID).

  36. DB:TYPES REGEN — first successful run, 19821 bytes.

  37. [BUG] DB:TYPES BREAKS BUILD ON null vs undefined. db:types models
     DEFAULT NULL params as `string | undefined` not `string | null`.
     Fix: pass undefined or `?? undefined` in optional RPC params.

  38. [BUG] CREATE_TENANT_WITH_OWNER COLUMN AMBIGUOUS. Root cause: OUT
     params shadow column refs (settings.tenant_id, ON CONFLICT target).
     Fix: patches/0002 with #variable_conflict use_column. First tenant
     'Eddy's Jewelers Corp.' created with tenant_id 1abc8070-...

  39. /ONBOARD CLAIM FLOW BUILT — page/content/actions/layout. Phase-1
     deferred item lifted into Phase 0 because it blocked smoke-test.

  40. [BUG] HYDRATION MISMATCH on flash-banner URL. Root cause: typeof
     window branch renders different strings server vs client. Fix:
     NEXT_PUBLIC_APP_URL inlined at build time.

  41. /ADMIN/TENANTS — OPEN BUTTONS ADDED. POST /api/tenant/switch then
     window.location.assign('/dashboard'). New i18n keys common.open /
     opening EN+ES.

  42. SESSION 2 BUILDS GREEN at 13 routes + Proxy. Dev server stable on
     :3060.

--- Session 3 (2026-04-26 ~18:00) — Phase 1 complete: Customers + Inventory ---

 43. PHASE 1 PLANNING. User chose to start Phase 1 directly. Proposed
     three-step plan: (1) schema + RLS + storage, (2) Customers module,
     (3) Inventory module. User confirmed two scope details: dual SKU
     fields (sku TEXT formatted + sku_number BIGINT raw), customer tags
     enabled. Pre-push hook re-pitch was declined; saved as
     feedback_no_prepush_hooks.md memory so it doesn't recur.

 44. PATCHES/0003-CUSTOMERS-INVENTORY.SQL written (~580 lines):
     - 9 enums (id_document_type, customer_doc_kind, comm_preference,
       inventory_category × 14 variants, inventory_source × 6,
       inventory_status × 6, metal_type × 12, inventory_location × 7,
       transfer_status × 4)
     - 7 tables: tenant_counters, customers, customer_documents,
       inventory_items, inventory_item_photos, inventory_item_stones,
       inventory_transfers
     - tenant_counters generic counter table + next_tenant_counter()
       function (used by inventory SKU now; pawn ticket / sale receipt /
       repair ticket numbers will reuse it)
     - inventory_items SKU auto-assign trigger: 'INV-' + LPAD(seq, 6, '0')
     - inventory_item_photos one-primary trigger demotes siblings
     - inventory_transfers cross-chain block trigger (must share parent
       tenant_id; standalone tenants can't transfer)
     - Storage buckets customer-documents + inventory-photos created
       INSERT INTO storage.buckets ... ON CONFLICT DO NOTHING
     - Storage RLS via (storage.foldername(name))[1]::UUID — first folder
       segment must be the tenant_id
     - Per-table RLS for staff via my_accessible_tenant_ids() +
       my_is_staff(tenant_id)
     - inventory_transfers dual-readable: both origin and destination
       shops see the row

 45. SCHEMA APPLIED + DB:TYPES REGEN (40968 bytes).

 46. DATABASE-ALIASES.TS UPDATED.

 47. PHASE 1 DEPS INSTALLED. zod, react-hook-form, @hookform/resolvers,
     @phosphor-icons/react.

 48. STORAGE HELPER (src/lib/supabase/storage.ts NEW). Path builders
     enforce tenant_id at folder[0].

 49. ZOD SCHEMAS — customer.ts + inventory.ts.

 50. PHASE 1 i18n KEYS EN+ES — ~120 mirrored keys.

 51. SIDEBAR COMPONENT. Phosphor icons. Module-gated entries.

 52. CUSTOMERFORMFIELDS + INVENTORYFORMFIELDS shared components.

 53. CUSTOMERS MODULE — 3 routes (list / new / [id]).

 54. INVENTORY MODULE — 3 routes.

 55. [BUG] CUSTOMER LANGUAGE TYPED AS STRING. Fix: narrow with ternary.

 56. [BUG] CUSTOMERFORMFIELDS Field LACKED placeholder PROP. Fix: added.

 57. PATCHES/0004-CUSTOMER-PAWN-FIELDS.SQL — 7 new optional pawn columns.

 58. PAWN FIELDS WIRED. UI gates by hasPawn prop.

 59. AUDIT LOG HELPER (src/lib/audit.ts NEW). Admin client + JSON narrow.

 60. AUDIT WIRED INTO ALL MUTATING ACTIONS.

 61. DASHBOARD WIDGETS. 4 stat cards + 2 recent panels.

 62. CUSTOMER PORTRAIT PHOTO. Round 96px avatar with hover-camera.

 63. [BUG] AUDIT changes COLUMN TYPE MISMATCH. Fix: JSON.parse(JSON.stringify(x)).

 64. SESSION 3 BUILDS GREEN at 19 routes + Proxy. Phase 1 = COMPLETE.

--- Session 4 (2026-04-27) — Phase 2 pawn loans + transfers UI + audit viewer ---

 65. PARALLEL-AGENT DISPATCH. Three agents in worktree isolation —
     Phase 2 / transfers / audit. All also wrote to primary tree by
     accident; happy-accident merge.

 66. PATCHES/0005-PAWN-LOANS.SQL written + applied (~346 lines).

 67. PHASE 2 PAWN MODULE — internal flow (no print, no portal).

 68. LIB/PAWN/MATH.TS decimal-safe loan math.

 69. CUSTOMER DETAIL + DASHBOARD WIRED for pawn.

 70. PATCHES/0006-TRANSFER-METADATA.SQL applied.

 71. INVENTORY TRANSFERS UI.

 72. INVENTORY EDIT/DELETE LOCKED on status='transferred'.

 73. AUDIT LOG VIEWER at /audit.

 74. LIB/AUDIT-FORMAT.TS separated from audit.ts (client-bundle hygiene).

 75. PER-TABLE AUDIT LABEL OVERRIDES.

 76. SCRIPTS/APPLY-MIGRATION.MJS (NEW). Supabase Management API CLI.

 77. DB:TYPES REGEN — 52,401 bytes.

 78. [BUG] FORM ENCTYPE REGRESSION. Fix: removed explicit attribute.

 79. [BUG] FORMDATA TYPING ERROR pawn/new/actions.ts:83. Fix: String(i).

 80. [BUG] AUTO-SPLIT useEffect in RecordPaymentDialog. Fix: useMemo.

 81. [BUG] 'use server' EXPORTS NON-ASYNC. Fix: dropped const exports,
     inline `{}` at useActionState call site. Commit a481a82.

 82. SESSION 4 BUILDS GREEN at 25 routes + Proxy.

 83. PARALLEL-AGENT DISPATCH (continued). Phase 3 dispatched alongside
     Session 4 closeout. Worktree cleanup PARTIAL — sandbox blocked
     remove --force / unlock / prune.

--- Session 5 (2026-04-27 ~late) — Phase 3 merge, Phase 2 PRINT PDF, Phase 4 RETAIL/POS, DL scanner ---

 84. PHASE 3 MERGE + 0007 APPLIED. patches/0007-repair-tickets.sql ran
     via apply-migration.mjs; types regenerated to 67,632 bytes
     (was 52,401). Two type fixes on agent output: RepairTicketUpdate +
     RepairTimeLogUpdate replaced Record<string, unknown> in three
     patches in /repair/[id]/actions.ts; TimerPanel useState wrapped
     in lazy `() => Date.now()` initializer (purity rule blocked
     direct call).

 85. WORKTREE CLEANUP COMPLETED. The three Session-4 orphans + a fourth
     stray (af80843a) all removed via `git worktree remove -f -f`
     (DOUBLE-force overrides agent locks). Branches deleted via
     `git branch -D`. The Session-4 sandbox blocks went away when
     given a proper terminal — single -f insufficient, double works.

 86. PHASE 3 PUSHED. Commit 6e8e799 — repair / stone-setting / sizing
     tickets. 28 routes.

 87. PARALLEL-AGENT DISPATCH (B + C). Phase 2 print PDF + Phase 4 POS
     dispatched as parallel agents in worktree isolation. Branches:
     worktree-agent-a2d4f582... (B) and worktree-agent-a5a5c61b... (C).
     Both stayed in their worktrees this time — no leak into primary.

 88. PHASE 2 PRINT PDF SHIPPED. Installed @react-pdf/renderer +
     @fontsource/inter + @fontsource/jetbrains-mono. PawnTicketPDF.tsx
     (~810 lines) renders one bilingual EN+ES ticket per loan with
     stacked-pair labels for fields and a two-column legal disclosure
     block. /api/print/loan/[id] route handler streams PDF via
     renderToBuffer. Print button on /pawn/[id] now opens the PDF in
     a new tab; first click also fires printTicketAction (locks core
     fields), reprints skip the action. Florida pawn statute is a
     placeholder pending counsel vetting; lives in
     pawn.print.legal.placeholder + pawn.print.legal.terms i18n keys
     so updates are translation patches not component rewrites.

 89. PHASE 4 RETAIL/POS SHIPPED. patches/0008-retail-pos.sql (~590
     lines). 6 enums (sale_status, sale_kind, return_status,
     layaway_status, register_session_status, card_present_status) +
     8 tables (register_sessions, sales, sale_items, sale_payments,
     returns, return_items, layaways, layaway_payments). Sale/return/
     layaway numbers via next_tenant_counter (S- / R- / L- 6-digit
     pad). Sales BEFORE UPDATE lock-enforcement trigger; partial
     unique index "one open register session per tenant".

 90. POS UI. /pos home (open/close register, today's sales),
     /pos/sales/new (cart with line items, customer picker, totals,
     save-as-layaway), /pos/sales/[id] (payments, status-gated
     buttons: Add Payment / Complete / Issue Return / Void / Print
     Receipt placeholder), /pos/returns/new (qty-capped at remaining,
     restock toggle per line), /pos/layaways list + detail (schedule
     preview, payments history, Add Payment + Cancel Layaway with
     fee preview). 14 audit verbs added.

 91. STRIPE TERMINAL STUB. lib/stripe/terminal.ts:
     createCardPresentPaymentIntent + cancelPaymentIntent +
     refundCardPayment against per-tenant Connect account from
     tenant_billing_settings. Throws tenant_stripe_not_connected
     when stripe_account_id missing. Reader pairing UI deferred —
     AddPaymentDialog shows yellow banner + reader_id free-text
     input + TEST mark-succeeded shortcut.

 92. POS LAYAWAY LIFECYCLE. Items go available → held at layaway
     creation, held → sold on balance-zero completion, held →
     available on cancel-with-restock. Cancellation fee captured as
     percent on layaway row; refund written as a negative-amount
     layaway_payment row (CHECK constraint is `amount <> 0`, not
     `> 0`).

 93. SALE + LAYAWAY 1:1. Every layaway has a parent sales row
     (sale_kind='layaway'). Layaway payments mirror onto sale_payments
     to keep sales.paid_total in sync without recompute pass.

 94. MIGRATION 0008 APPLIED + DB:TYPES REGEN — 87,736 bytes (up from
     67,632).

 95. POST-MERGE TYPE FIXES (4). FlaskConical → Flask (Phosphor doesn't
     export FlaskConical — that's Lucide). Map<string, typeof x extends
     Array<infer R> ? R : never> collapses to never when nullable;
     replaced with NonNullable<typeof x>[number]. layawayDraft.discount
     + .taxRate are string-typed form values — parsed via parseFloat
     before computeTotal. Same fix in Cart.tsx useMemo.

 96. PHASE 4 PUSHED. Commit 7902805 — fix-up after B + C merges.
     34 routes.

 97. VERCEL PROJECT IMPORTED. Eddy created Vercel project + uploaded
     .env.vercel.import. The 12 runtime env keys imported;
     SUPABASE_ACCESS_TOKEN + SUPABASE_PROJECT_ID excluded as dev-only
     CLI tools. Import file deleted from disk after upload.

 98. DL SCANNER PORT-KIT INTEGRATED. AAMVA PDF417 driver-license
     scanner from C:\Users\rodri\OneDrive\Documents\Claude\code\
     dl-scanner\. Adapted to project: @phosphor-icons/react Scan + X
     icons (kit shipped lucide-react), project palette (rausch / ink /
     canvas / hairline / ash / error), bilingual via t.dlScanner.*
     (9 keys). useEffect mount-state dropped in favor of `typeof
     document === 'undefined'` guard (avoids the
     react-hooks/set-state-in-effect rule). Wired into /customers/new
     with key-bump remount pattern (CustomerFormFields stays
     uncontrolled). Field map: AAMVA → first/middle/last/dob/sex/
     id_number/id_expiry/address1/city/state/zip/height_inches; forces
     id_type='drivers_license' + id_country='US'; license-issuing
     state mirrored into both state + id_state. Raw payload carried
     in hidden dl_raw_payload input (server action ignores until
     column lands). parseHeightInches handles "071 in" + "180 cm".

 99. DL SCANNER PUSHED. Commit b4679f4.

100. SESSION 5 BUILDS GREEN at 34 routes + Proxy. Phase 4 (internal)
     COMPLETE. Phase 3 COMPLETE. Phase 2 (with print) COMPLETE. Lint
     clean except the carry-over Phase 0 use-lang.ts:29.

101. [BUG OPEN] /customers/new save fails silently. Eddy reported
     during smoke testing. Diagnostic asked (browser console / red
     error banner / DevTools network response) — Eddy moved on to
     Vercel import without confirming. Hypothesis:
     optionalTrimmedString in src/lib/validations/customer.ts runs
     z.string().min(1) AFTER the trim preprocess, so empty optional
     fields fail with a min(1) error. Form returns fieldErrors but
     renders inline next to each field, not in the top banner —
     easy to miss. Diagnose first thing next session (see RESUME
     POINT for the fix path).

================================================================================
PENDING — NEXT UP
================================================================================

  IMMEDIATE (do this first next session):
    Diagnose the "/customers/new save fails silently" bug. Hypothesis
    is the optionalTrimmedString schema. Confirm by opening DevTools
    Network tab, submitting a customer, looking at the action's POST
    response. Fix: rewrite optionalTrimmedString in
    src/lib/validations/customer.ts to convert empty-after-trim to
    null BEFORE the min(1) check, so empty optional fields parse
    cleanly. Re-test.

  PHASE 0 PUNCH LIST (still open):
    a. Confirm Vercel deploy status + production URL.
    b. Update NEXT_PUBLIC_APP_URL in .env.local + Vercel (Production
       env). Redeploy.
    c. Rotate the three pasted secrets (anon JWT, service_role JWT,
       sbp_ access token). Update .env.local + Vercel.
    d. Optionally revert repo-local git user.name + user.email config
       (CLAUDE.md violation — use inline `-c` overrides going forward).

  BACKLOG (in priority order):
    1. Phase 0 close-out (deploy verify + secret rotation + URL update).
    2. Phase 5 — Customer portal /portal/loans + Stripe Payment Link
       payoff (high customer-visible value; loan side ready).
    3. Phase 6 — Communications (Twilio SMS / WhatsApp / Resend per-
       tenant). Pawn maturity reminders, repair-ready notifications,
       layaway reminders. Bilingual.
    4. Phase 7 — Reporting + FL LeadsOnline export.
    5. Phase 8 — Polish.
    6. DL scanner upgrades — dl_raw_payload audit column,
       block-expired-licenses guard, front-of-license webcam capture.
    7. POS receipt PDF (mirrors /api/print/loan pattern).
    8. Stripe Terminal hardware reader pairing UI when readers arrive.
    9. /magic-link, /set-password auth pages stubs (Phase 5 blocker).

  KNOWN GOTCHAS TO AVOID FROM DAY 1 (carried + new):
    - Next 16 file is `proxy.ts`, exported function named `proxy`.
    - Next 16 'use server' modules can ONLY export async functions —
      never `export const ...InitialState`. Inline the value at the
      useActionState call site. Type exports are fine.
    - Phosphor icons crash server components — only import in 'use
      client' files. (Sidebar, all panels, all forms are clients.)
    - Phosphor doesn't have FlaskConical — that's Lucide. Use Flask.
    - useSearchParams() must be wrapped in <Suspense> for static gen.
    - <form action={fn}> never has explicit encType. The runtime
      sets multipart automatically when a file input is present.
    - Storage uploads from server actions use admin (service-role)
      client. uploadToBucket helper handles this.
    - `npm run db:types` via the safe wrapper. Never raw `>` redirect.
    - Service role key NEVER goes in NEXT_PUBLIC_*.
    - DATABASE_URL via Supavisor pooler (not direct DB host).
    - GitHub commits MUST be authored by the identity Vercel has linked.
      Use `git -c user.name=... -c user.email=...` for inline override.
    - git worktree cleanup uses `-f -f` (DOUBLE-force) to override
      agent locks. `git worktree prune` after.
    - Auth callback needs both PKCE AND implicit-flow handling.
    - Portal invites: don't use Supabase built-in SMTP.
    - profiles.role is GLOBAL only ('superadmin' | NULL).
    - user_tenants has no FK to profiles — embedded select returns null.
    - audit_log.user_id has no FK to profiles either — same gotcha,
      same fix (explicit IN-query merge instead of embedded select).
    - Never taskkill /F /IM node.exe — use PIDs.
    - audit_log has SELECT policy only; inserts must use admin client.
    - CHECK-constrained TEXT columns (e.g. profiles.language,
      customers.language) come back as plain string from db:types,
      not as the enum literal — narrow at the page boundary.
    - Supabase JSONB columns are typed as a strict Json union;
      Record<string, unknown> needs JSON-roundtrip narrowing.
    - Supabase update(patch) types reject Record<string, unknown> —
      type the patch as the table's Update alias instead.
    - First uploaded inventory photo auto-promotes to primary.
      Deleting the primary auto-promotes the next-positioned photo.
    - Storage path layout: tenant_id MUST be folder[0] for RLS to work.
    - Pawn ticket print locks core fields — only status='extended'
      transitions are allowed past is_printed=true.
    - Forfeiture inventory items start status='available' (not held).
    - Repair abandonment items start status='available' (same hook
      pattern).
    - Inventory edit/delete blocked while status='transferred' or
      ='sold' or ='scrapped'.
    - Cross-chain transfers blocked at trigger; surface as
      'cross_chain_blocked' field error in the form.
    - Use useMemo for derived state, not useEffect+setState.
    - Date.now() / random / location / etc. inside useState() trip
      the react-hooks/purity rule. Wrap in lazy initializer:
      useState(() => Date.now()).
    - lib/audit.ts pulls in the admin client — never import it from a
      client component. Use lib/audit-format.ts for label helpers.
    - layaway_payments.amount allows NEGATIVE values (cancellation
      refund as reversing entry). CHECK constraint is `amount <> 0`.
    - Stripe Connect: every per-tenant call needs Stripe-Account
      header. Helper in lib/stripe/terminal.ts handles this; don't
      forget on future Stripe wrappers.
    - @fontsource fonts in @react-pdf/renderer must be loaded via
      fs.readFileSync + base64 dataURL — not require()/import. The
      Turbopack static analysis stripper otherwise breaks the file
      paths.

  KNOWN ISSUES TO REVISIT WHEN PHASES LAND:
    - Phase 5 portal: pawn ticket print PDF legal disclosure — vet
      with counsel before any live FL pawn intake. Lives in
      pawn.print.legal.* i18n keys.
    - Phase 5: compliance_log.customer_snapshot must include the new
      pawn fields. Verify the snapshot the intake action writes
      today still captures height/weight/marks/sex/hair/eyes/
      employment.
    - Phase 4: Stripe Terminal hardware — confirm reader model + cost
      and ship `@stripe/terminal-js` pairing UI.
    - Phase 7: confirm current FL LeadsOnline upload format (drifts).
    - ID retention enforcement: read current FL statute right before
      portal pay-by-link ships.
    - DL scanner: dl_raw_payload TEXT column for compliance audit
      (form already POSTs the value; server ignores until column lands).
    - DL scanner: block-expired-licenses guard inside
      createCustomerAction. Parser returns expirationDate already.
    - DL scanner: front-of-license photo capture (webcam) for
      jurisdictions that require both PDF417 + visual ID.

  OUT OF SCOPE (noted for later):
    - Mobile native app.
    - Cross-tenant customer matching (blacklist-share).
    - Watch / fine-jewelry authentication services.
    - eBay / Worthy listing integration.
    - Bullion / coin spot-price feeds.
    - Wholesale buy-side (gold-buying with melt-value calculator).
================================================================================
