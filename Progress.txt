================================================================================
PROJECT: Pawn — Multi-Tenant Pawn / Jewelry / Repair SaaS
Operator: Rodriguez Multi Service LLC (Eddy Rodriguez)
File:    Progress.txt
================================================================================

SEE ALSO (do not duplicate here):
  - Architecture / conventions: CLAUDE.md
  - Source of truth: the code itself

────────────────────────────────────────────────────────────────────────────────
│  RESUME HERE — START EVERY NEW SESSION BY READING THIS BLOCK                │
────────────────────────────────────────────────────────────────────────────────

  Last session:   Session 6 (2026-04-27 evening) — Phases 5 + 6 + 7 all
                  merged + customer-save bug fix + dashboard port.
                  Build green at 50+ routes.
  Context budget: HEAVY. Save and clear before next session.
  Last worked on: Three-phase parallel agent dispatch (5/6/7) with
                  worktree isolation + sequential merges. Customer-save
                  bug FIXED. Vibrant-dark dashboard ported into
                  /staff/dashboard. All four pushes live on Vercel.

  ACTIVE FILE(S) — last touched this session:
    --- Customer-save fix ---
    - src/lib/validations/{customer,inventory,loan,pos,repair,transfer}.ts
      (preprocess returns null on empty-trim BEFORE inner schema)
    - src/app/(staff)/customers/new/form.tsx (top banner on fieldErrors)
    - src/lib/i18n/{en,es}.ts (common.fixErrorsBelow)

    --- Dashboard port ---
    - src/app/(staff)/dashboard/dashboard.css (NEW — scoped under
      .dashboard so dark vars don't bleed into Airbnb-light surfaces)
    - src/app/(staff)/dashboard/content.tsx (full rewrite — 5 tabs,
      autocomplete search, recent chips, DlScanner, charts, kbd shortcuts)
    - src/app/(staff)/dashboard/actions.ts (NEW — searchAcrossModule
      server action against loans/sales/layaways/repair_tickets)

    --- Phase 5 merged (worktree-agent-a0c62 → main a9b4995) ---
    - patches/0009-customer-portal.sql (NOT APPLIED)
    - src/app/(auth)/{magic-link,set-password}/* (NEW)
    - src/app/(portal)/portal/{loans,layaways,repairs}/* (NEW)
    - src/app/api/stripe/webhook/route.ts (NEW)
    - src/lib/stripe/payment-link.ts (NEW)
    - src/lib/portal/* (NEW)
    - src/components/portal/PortalChrome.tsx (NEW)
    - src/types/database-aliases.ts (Stripe payment link types added)

    --- Phase 6 merged (worktree-agent-a325 → main 0519b51) ---
    - patches/0010-communications.sql (NOT APPLIED — extends settings
      table + adds message_templates + message_log + 66 seed templates)
    - src/lib/twilio/{sms,whatsapp,templates}.ts (NEW)
    - src/lib/email/send.ts (NEW)
    - src/lib/comms/{render,dispatch}.ts (NEW)
    - src/app/api/cron/{maturity,repair-ready,layaway}-reminders/route.ts
    - src/app/(staff)/settings/communications/* (NEW)
    - src/components/comms/{CredsForm,TemplateEditor,TestSendDialog,
      SendReminderDialog}.tsx (NEW)
    - SendReminderDialog wired into pawn/repair/layaway detail pages

    --- Phase 7 merged (worktree-agent-a24d → main fd39163) ---
    - patches/0011-reporting.sql (NO-OP HEADER — apply or skip)
    - src/lib/reports/* (8 query helpers)
    - src/lib/compliance/{police-report/index.ts, retention.ts,
      formats/fl-leadsonline.ts} (NEW)
    - src/lib/pdf/reports/ReportPDF.tsx (NEW)
    - src/app/(staff)/reports/* (8 report pages + landing)
    - src/app/api/reports/* (15 PDF + CSV streamers)
    - src/components/reports/{DateRangePicker,ExportButtons,
      ReportPageShell,ReportTable}.tsx (NEW)
    - src/lib/format/money.ts (NEW)
    - src/lib/audit.ts (added 'export' AuditAction)

  RESUME POINT:
    Apply the three phase migrations against Supabase IN ORDER, then
    regenerate types and collapse the manual hand-rolled aliases.

      1. patches/0009-customer-portal.sql
         (customers.auth_user_id, stripe_payment_links table,
          client-role RLS, get_my_customer_id() helper)
      2. patches/0010-communications.sql
         (extends settings with 5 comms cols + message_templates +
          message_log + 66 seed templates for first tenant)
      3. patches/0011-reporting.sql
         (NO-OP — header only; apply for completeness or skip)

    Use scripts/apply-migration.mjs OR Supabase SQL Editor. Then:
      - npm run db:types  (regen src/types/database.ts)
      - Collapse hand-rolled types in src/types/database-aliases.ts:
          StripePaymentLinkRow/Insert/Update + StripePaymentLinkKind/Status
          MessageTemplateRow/Insert/Update + MessageKind/Channel/Status
          MessageLogRow/Insert/Update
          SettingsCommsColumns
        These can collapse to Database['public']['Tables']['…']['Row']
        forms (field names deliberately identical).
      - Drop the `as unknown as { from: ... }` casts in src/lib/twilio/*,
        src/lib/email/send.ts, src/lib/comms/dispatch.ts, the cron routes,
        and the settings/communications page (Phase 6 agent flagged these).

    Verify npm run build green. Then pick next workstream.

  OPEN ISSUES / BLOCKERS:
    - 3 phase migrations (0009 / 0010 / 0011) NOT YET APPLIED. Operator
      action required before any of the merged code talks to the DB.
    - Three pasted secrets STILL pending rotation (anon JWT, service-role
      JWT, sbp_ access token). Phase 0 leftover.
    - NEXT_PUBLIC_APP_URL still localhost:3060 in env (Vercel + .env.local).
    - Stripe Connect not yet onboarded — portal payoff + comm cron silently
      no-op until tenant_billing_settings.stripe_account_id is populated.
    - DATABASE_URL still empty in .env.local (low priority).
    - Pawn print PDF legal disclosure placeholder — counsel vetting needed
      before live FL pawn intake.
    - WhatsApp Content SIDs are NULL placeholders — needs Twilio + Meta
      approval per template per tenant.
    - pgsodium at-rest encryption deferred for Twilio/Resend creds.
    - FL LeadsOnline CSV format is DRAFT — confirm with agency before
      go-live (header has a "FORMAT DRAFT — confirm" comment).
    - tenants.agency_store_id column missing — police-report falls back
      to tenant UUID. Single ADD COLUMN migration when convenient.
    - Pre-existing use-lang.ts:29 lint error from Phase 0 still unresolved.
    - Repo-local git config (user.name/email) decision pending — keep or
      revert. CLAUDE.md global rule says no `git config` writes.
    - DL scanner upgrades deferred: dl_raw_payload column, expired-license
      block, webcam capture.
    - Old t.dashboard.* i18n keys (customersCard, inventoryCard, etc.)
      now unused — cleanup later.
    - Dashboard hero "Pawn today" / "Repairs today" stats show '—'
      placeholders; need page.tsx extensions to compute.
    - Dashboard chart series run on deterministic demo PRNG data — wire
      to Phase 7 queries when types collapse.
    - Dashboard customer-name search not yet joined into autocomplete
      (only ticket numbers match today).
    - "Banner-on-fieldErrors" pattern only on /customers/new so far —
      candidates for rollout: /inventory/new, /pawn/new, /repair/new,
      /pos/sales/new, /inventory/transfers/new.

  KEY DECISIONS LOCKED THIS SESSION:
    - DASHBOARD AS A SECOND VISUAL SURFACE. Airbnb-light remains
      canonical for the rest of the app per DESIGN-airbnb.md, but
      /staff/dashboard ships as a vibrant-dark second surface
      (per-module signature gradients: violet/cyan/indigo/amber/emerald,
      JetBrains Mono numerics, inline SVG charts). Style scoping
      discipline: ALL dashboard CSS lives under .dashboard parent
      selector so dark tokens never bleed. :root vars + global body/*
      resets from the original kit explicitly hoisted into .dashboard
      to prevent leak. Future second-surface contenders (e.g. POS
      register screens) follow the same scoping pattern.
    - DASHBOARD localStorage CONVENTION. Prefix `dash.*` for state
      (activeTab, activeRange, recent.{module}). Lazy initializers with
      typeof window guards in useState() keep SSR safe AND dodge
      react-hooks/set-state-in-effect — same pattern Session 5 locked
      for Date.now() / random / location and the DL scanner mount
      detection.
    - DASHBOARD SEARCH SHIPS WITHOUT CUSTOMER-NAME MATCH. v1 searches
      by ticket_number / sale_number / layaway_number only via
      Supabase ILIKE. Customer-name search needs a join + de-dup;
      deferred. Status pills are tone-coded (success/warning/error/
      muted) with module-specific status enums.
    - AGENTS COMMIT-PER-WORKTREE; OPERATOR MERGES. Pattern confirmed
      AGAIN: dispatch agents with Agent tool isolation: 'worktree',
      agent commits inside its worktree, primary tree merges with
      `git merge --no-ff <branch>`, conflicts resolved at merge time,
      `git worktree remove -f -f` + `git branch -D` cleans up after.
      Three Session 6 agents followed this. The first Phase 7 agent
      crashed mid-work (system kernel crash) — re-dispatch went
      cleanly. Lesson: even on crash, the worktree branch survives
      with whatever was committed; only uncommitted work is lost.
    - 3-WAY MERGE OF ADDITIVE TOP-LEVEL OBJECTS = ALWAYS UNION. Pattern
      seen at every Session 6 merge:
        Phase 6 merge → src/types/database-aliases.ts: HEAD added
          StripePaymentLink* shapes; branch added Message* + Settings
          CommsColumns. Both at end of file. Union = both blocks.
        Phase 7 merge → src/lib/i18n/{en,es}.ts: HEAD's last namespace
          was `comms`; branch's last namespaces were `reports` and
          `compliance`. Conflict region encompassed both sides' contents
          (without their closing `},`). Union = comms + reports +
          compliance, all properly closed.
      Never pick one side; always union the additions.
    - VALIDATION HELPER FIX SHAPE LOCKED. optionalTrimmedString and
      similar (email, optionalShortString) must convert empty-after-trim
      to null in PREPROCESS, BEFORE the inner z.string().min(1) check.
      Trailing `.transform((v) => v ?? null)` keeps the output type
      `string | null` so Supabase Insert types still match. Applied
      across customer/inventory/loan/pos/repair/transfer schemas. The
      bug had been silently failing /customers/new in Session 5; root
      cause: empty optional FormData strings hit min(1) before the
      transform could null them.
    - BUG-FIX BANNER PATTERN. When a server action returns
      { fieldErrors } without a top-level state.error, surface a
      generic "Please fix the highlighted fields below" banner at the
      top of the form so silent inline-only validation failures can't
      be missed. New i18n key common.fixErrorsBelow EN+ES. Pattern
      candidates for rollout: /inventory/new, /pawn/new, /repair/new,
      /pos/sales/new, /inventory/transfers/new.
    - PHASE 5 — STRIPE PAYMENT LINK + AUTH FLOW SHIPPED. Customer
      portal at /portal/{loans,layaways,repairs} gates on
      tenantRole='client'. Magic-link + set-password auth pages now
      stubbed (Phase 0 blocker resolved). Customers.auth_user_id links
      portal users to their customer row. Stripe webhook handler at
      /api/stripe/webhook handles checkout.session.completed for both
      kinds (loan_payoff, layaway_payment) idempotently. Payment link
      helpers at lib/stripe/payment-link.ts (per-tenant Connect
      account, Stripe-Account header).
    - PHASE 6 — EXTENDED SETTINGS TABLE (NOT NEW TABLE) for Twilio +
      Resend creds. Settings already had Twilio/Resend cols from
      0001-foundation; 0010 adds 5 new canonical cols
      (twilio_sms_from, twilio_whatsapp_from, twilio_messaging_service_sid,
      resend_from_email, resend_from_name) and keeps legacy cols for
      fallback. message_log has a write-once trigger — only status /
      provider_id / sent_at / delivered_at / error_text are updatable.
      66 seed templates for first tenant (11 kinds × 2 langs × 3 channels).
      sendXxx provider helpers silently no-op + log when creds missing
      OR NODE_ENV='test'.
    - PHASE 7 — POLICE REPORT READS compliance_log ONLY (Rule 15
      enforced). FL LeadsOnline CSV exporter marks rows
      exported_at / exported_format / exported_batch_id per batch.
      No idempotency check on re-export; acceptable for v1.
      lib/compliance/retention.ts ships RETENTION_RULES (FL: 2y pawn /
      30d buy-hold) + canDeleteCustomer() helper (block reasons
      structured for the future customer DELETE guard). v1 ships
      against existing schema only — patches/0011-reporting.sql is a
      reserved no-op header.
    - DASHBOARD STORAGE PATH FOR RECENTS. localStorage key shape:
      dash.recent.{sales|layaways|pawn|repairs}, JSON array of
      {id, label, href}, max 5, deduped on add. SSR-safe via
      typeof window guard inside readStoredJson() helper.

  KEY DECISIONS CARRIED FROM PRIOR SESSIONS (still authoritative):
    - Multi-store from day 1 (tenants.parent_tenant_id + tenant_type).
    - Per-tenant module gates: has_pawn / has_repair / has_retail.
    - Stone setting = service_type ENUM on repair tickets.
    - FL LeadsOnline only for v1.
    - Stripe: Connect + Terminal + Payment Links from one account.
    - compliance_log is write-once. Police-report exporter reads from
      it (not loans/sales).
    - Pawn ticket immutability after print (status='extended' is the
      one allowed post-print mutation).
    - Dedicated cloud projects (Rule 17): own GitHub + Supabase + Vercel.
    - NEXT_PUBLIC_APP_URL is the canonical origin source.
    - window.location.assign over router.push for cookie-set navs.
    - Lookup-by-email pattern for inserting profiles tied to auth.users.
    - DUAL SKU FIELDS (sku TEXT + sku_number BIGINT).
    - PAWN FIELDS GATE BY has_pawn IN UI ONLY (schema columns always exist).
    - AUDIT_LOG INSERTS THROUGH ADMIN CLIENT (no INSERT policy on table).
    - JSON-roundtrip narrowing for Supabase JSONB columns.
    - Photo storage path: tenant_id MUST be folder[0] for RLS to work.
    - 'use server' modules export async functions ONLY — never const
      objects. Inline `{}` at the useActionState call site.
    - Forfeit-to-inventory hook (loans → inventory_items source=
      'pawn_forfeit'); abandoned-to-inventory hook (repair_tickets →
      inventory_items source='abandoned_repair').
    - Decimal-safe math (r4 to 4dp) in lib/pawn/math.ts +
      lib/pos/cart.ts + lib/repair/billing.ts. NO floats.
    - Parallel-agent worktree pattern — agent commits on its branch;
      operator merges via `git merge --no-ff` from primary tree;
      cleanup via `git worktree remove -f -f` + `git branch -D`.

  IMPORTANT CONSTANTS / PATHS:
    - Project root:   c:\Users\rodri\OneDrive\Documents\Pawn\
    - Dev port:       3060
    - Sister apps:    Luna Azul (:3000) — c:\...\LunaAzul Web Sass\
                      Abacus     (:3030) — C:\...\Abacus\
    - Memory dir:     C:\Users\rodri\.claude\projects\
                      c--Users-rodri-OneDrive-Documents-Pawn\memory\
    - Active-tenant cookie: pawn-active-tenant
    - Storage layout:
        customer-documents/<tenant_id>/<customer_id>/<kind>/<uuid>.<ext>
        inventory-photos/<tenant_id>/<item_id>/<uuid>.<ext>
        repair-photos/<tenant_id>/<ticket_id>/<kind>/<uuid>.<ext>
    - Number formats:
        SKU                 'INV-'  + LPAD(seq,6,'0')   (INV-000123)
        Pawn ticket         'PT-'   + LPAD(seq,6,'0')   (PT-000123)
        Repair ticket       'RT-'   + LPAD(seq,6,'0')   (RT-000123)
        Sale                'S-'    + LPAD(seq,6,'0')   (S-000123)
        Return              'R-'    + LPAD(seq,6,'0')   (R-000123)
        Layaway             'L-'    + LPAD(seq,6,'0')   (L-000123)
    - First tenant: Eddy's Jewelers Corp.
        tenant_id    1abc8070-0797-4740-8dea-70cbb16060fe
    - DL scanner kit source:
        C:\Users\rodri\OneDrive\Documents\Claude\code\dl-scanner\
    - Dashboard kit source: uploaded inline Session 6 (CSS + JS + HTML
      + HANDOFF.md). Ported into /staff/dashboard.
    - Dashboard storage keys: dash.activeTab, dash.activeRange,
      dash.recent.{sales|layaways|pawn|repairs}.

================================================================================
PHASE OVERVIEW
================================================================================

  Phase   Description                                Status      Completed
  ------  -----------------------------------------  ----------  ------------
  0       Foundation skeleton                        IN PROGRESS  —
          (~92% complete: scaffold, schema, RLS,
          three layouts, auth pages, tenant
          provisioning, sidebar, Vercel project +
          env imported. Magic-link + set-password
          auth pages SHIPPED in Phase 5. Outstanding:
          deploy URL update, secret rotation.
          Pre-push hooks DECLINED.)
  1       Customers + Inventory                      COMPLETE    2026-04-26
  2       Pawn loans (gated on has_pawn)             COMPLETE    2026-04-27
          (Bilingual EN+ES print PDF Session 5.)
  3       Repair / stone setting                     COMPLETE    2026-04-27
  4       Retail / POS                               COMPLETE    2026-04-27
  5       Customer portal (full)                     COMPLETE    2026-04-27
          (Session 6: /portal/loans + /portal/layaways
          + /portal/repairs + Stripe Payment Link
          payoff + magic-link/set-password auth +
          customers.auth_user_id link + client-role
          RLS. patches/0009 NOT YET APPLIED.)
  6       Communications                             COMPLETE    2026-04-27
          (Session 6: per-tenant Twilio SMS+WhatsApp
          + Resend, message_templates + message_log,
          3 cron routes, settings UI, SendReminder
          Dialog wired into 3 detail pages. patches/
          0010 NOT YET APPLIED. WhatsApp content SIDs
          pending Twilio+Meta approval.)
  7       Reporting + compliance                     COMPLETE    2026-04-27
          (Session 6: 8 reports w/ CSV+PDF, FL Leads
          Online exporter draft, retention helper.
          patches/0011 is a no-op header — apply or
          skip. Format DRAFT — confirm w/ agency.)
  8       Polish                                     PENDING     —
          (Mobile audits, real Stripe smoke test,
          dashboard real-data wire-up after types
          regen, banner pattern rollout to other
          forms, customer-name search in autocomplete,
          old t.dashboard.* keys cleanup, hero stats
          pawn-today/repairs-today computation.)

================================================================================
SESSION HISTORY LOG
================================================================================

--- Session 1 (2026-04-26) — Project planning + skeleton docs ---

  1. ANALYZED LUNA AZUL + ABACUS PROGRESS LOGS. Pulled forward multi-tenant
     architecture, RLS helper pattern, getCtx, guards, dev-watchdog,
     Next 16 proxy.ts, db:types safe wrapper, etc.

  2. PROPOSED 11 DOMAIN MODULES (folded stone setting into repair).

  3. DECISIONS LOCKED — multi-store from day 1, FL police-report only v1,
     Stripe Terminal + Payment Links, pawn module gated, dev port 3060.

  4-7. WROTE INITIAL CLAUDE.md + Progress.txt + DESIGN-airbnb.md, locked
     design tokens, semantic-color extension (Success Green / Warning
     Amber matched to Airbnb's #c13515 saturation), photography guidance
     adapted for pawn/jewelry domain, Cloud-isolation Rule 17.

  8-11. SEMANTIC-COLOR + PHOTOGRAPHY + CLOUD-ISOLATION decisions resolved.

  12-15. SCAFFOLD via npx create-next-app to subdir + mv merge. Watchdog
     cloned to :3060. Design tokens locked in tokens.ts. next.config.ts
     turbopack.root pinned to __dirname.

  16. PATCHES/0001-FOUNDATION.SQL written (~470 lines): 3 enums, 8 tables,
     5 RLS helpers, 3 RPCs, immutability triggers.

  17-23. SUPABASE CLIENT LIBRARY, PROXY.TS, I18N skeleton, AUTH PAGES,
     THREE LAYOUTS, /admin/tenants list+new, placeholder database.ts.

  24-26. SCRIPTS/DB-TYPES.MJS safe wrapper, .env.local.example, BUILD
     VERIFIED GREEN at 11 routes.

  27. CLAUDE.MD CLOBBER + RESTORE.

  28. PHASE 0 ~70% COMPLETE; HAND-OFF for cloud setup.

--- Session 2 (2026-04-26 ~16:00) — Cloud wiring + first tenant + bug fixes ---

  29-42. .ENV.LOCAL POPULATED. TenantSwitcher built. .gitignore extended.
     Git initialized + initial commit. Schema applied. Superadmin
     onboarded. db:types regen. Bugs fixed (db:types null vs undefined,
     create_tenant_with_owner column ambiguity, hydration mismatch on
     flash-banner URL). /onboard claim flow built. Open buttons added.
     Session 2 builds green at 13 routes.

--- Session 3 (2026-04-26 ~18:00) — Phase 1 complete: Customers + Inventory ---

  43-64. Phase 1 planning (dual SKU fields + customer tags). Patches/
     0003-customers-inventory.sql written + applied. Phase 1 deps
     installed. Storage helper. Zod schemas. i18n keys EN+ES. Sidebar
     with Phosphor icons. CustomerFormFields + InventoryFormFields.
     Customers + Inventory modules. 3 bug fixes. Patches/0004-customer-
     pawn-fields.sql. Audit log helper + wired everywhere. Dashboard
     widgets. Customer portrait photo. Audit JSONB type fix. Builds
     green at 19 routes.

--- Session 4 (2026-04-27) — Phase 2 pawn loans + transfers UI + audit viewer ---

  65-83. Parallel-agent dispatch (3 agents). Patches/0005 + 0006
     applied. Phase 2 pawn module — internal flow. lib/pawn/math.ts.
     Customer detail + dashboard wired. Inventory transfers UI.
     Inventory edit/delete locked on transferred. Audit log viewer
     at /audit. lib/audit-format.ts split. Per-table audit label
     overrides. Apply-migration.mjs. db:types regen. 4 bug fixes.
     Builds green at 25 routes. Phase 3 dispatched alongside closeout.

--- Session 5 (2026-04-27 ~late) — Phase 3 merge, Phase 2 PRINT PDF, Phase 4 RETAIL/POS, DL scanner ---

  84-101. Phase 3 merge + 0007 applied. Worktree cleanup completed
     (double -f). Phase 3 pushed at 6e8e799. Parallel-agent dispatch
     B+C (print PDF + POS). Phase 2 print PDF shipped (bilingual
     EN+ES, Florida statute placeholder). Phase 4 retail/POS shipped
     (0008 + full module). POS UI: register / sales / returns /
     layaways. Stripe Terminal stub. POS layaway lifecycle.
     Sale+layaway 1:1. 0008 applied + db:types regen (87,736 bytes).
     Post-merge type fixes (4). Phase 4 pushed at 7902805. Vercel
     project imported (12 runtime keys, 2 dev-only excluded). DL
     scanner port-kit integrated on /customers/new (key-bump remount,
     AAMVA → field map, scoped Phosphor icons + project palette).
     Builds green at 34 routes. [BUG OPEN] /customers/new save fails
     silently — diagnose first thing next session.

--- Session 6 (2026-04-27 evening) — Phases 5+6+7 merged + customer-save fix + dashboard port ---

 102. CUSTOMER-SAVE SILENT FAIL — DIAGNOSED + FIXED + ROLLED OUT.
     Hypothesis from Session 5 confirmed. Root cause:
     optionalTrimmedString in src/lib/validations/customer.ts ran
     z.string().min(1) AFTER the trim preprocess, so empty-after-trim
     strings failed validation BEFORE the trailing .transform could
     null them. Inline-only fieldErrors landed on fields deep in the
     pawn section (height_inches, weight_lbs) — easy to miss, looked
     silent. Fix: preprocess returns null when trimmed === '', BEFORE
     the inner schema. Trailing .transform((v) => v ?? null) keeps
     the output type `string | null` so Supabase Insert types match.
     Same pattern existed in 5 sibling validation files (inventory /
     loan / pos / repair / transfer) — all 6 patched together. Same
     fix applied to the email schema in customer.ts. Banner fix in
     /customers/new form: when fieldErrors is non-empty but
     state.error is null, surface "Please fix the highlighted fields
     below" at the top so silent inline-only failures can't slip
     past. New i18n key common.fixErrorsBelow EN+ES. Commit 56d054b.

 103. PARALLEL-AGENT DISPATCH SESSION 6. Three agents in worktree
     isolation: Phase 5 (portal), Phase 6 (comms), Phase 7 (reports
     + FL LeadsOnline). Phase 5 + 6 completed cleanly. First Phase
     7 agent CRASHED mid-work (system kernel crash, not a code
     issue) — partial work was 1500 lines of report query helpers
     in src/lib/reports/, fully untracked. Eddy chose option (b):
     discard partial. Worktree removed via double-force; fresh
     re-dispatch landed cleanly at 4d95aeb.

 104. PHASE 5 SHIPPED + MERGED. Branch worktree-agent-a0c62 →
     main a9b4995. 33 files, ~4250 lines. Adds:
       - patches/0009-customer-portal.sql (NOT APPLIED).
         customers.auth_user_id NULL FK to auth.users, stripe_payment_
         links table, get_my_customer_id() helper, client-role RLS
         policies on loans / loan_payments / repair_tickets / layaways /
         layaway_payments / sales / sale_payments scoped via
         customers.auth_user_id = auth.uid().
       - /portal layout (gates on tenantRole='client', simple top bar +
         lang switcher + sign-out, mobile bottom-fixed nav).
       - /portal/loans + /portal/loans/[id] (active loans, payoff
         balance, Pay-payoff CTA → Stripe Checkout via Payment Link
         on per-tenant Connect account; client_reference_id=loan_id,
         metadata={tenant_id, loan_id, kind='loan_payoff'}).
       - /portal/layaways + /portal/layaways/[id] (same pay-by-link
         pattern, kind='layaway_payment').
       - /portal/repairs (read-only status, photos when ready, signed
         URLs only).
       - /api/stripe/webhook (handles checkout.session.completed for
         both kinds idempotently — checks stripe_session_id before
         insert).
       - Auth blockers RESOLVED: /magic-link + /set-password pages +
         actions stubbed (Phase 0 blocker for portal invites).
       - lib/stripe/payment-link.ts (per-tenant Connect, Stripe-Account
         header). lib/portal/* helpers. PortalChrome component.
       - Hand-rolled types in database-aliases.ts (StripePaymentLink*)
         until 0009 applies + db:types regenerates.

 105. PHASE 6 SHIPPED + MERGED. Branch worktree-agent-a325 → main
     0519b51 (with 1 conflict resolution in database-aliases.ts).
     Adds:
       - patches/0010-communications.sql (NOT APPLIED).
         Schema choice: extended existing settings table (5 new
         canonical comms cols: twilio_sms_from, twilio_whatsapp_from,
         twilio_messaging_service_sid, resend_from_email,
         resend_from_name) rather than parallel
         tenant_communication_settings table — settings already had
         Twilio/Resend cols + owner-write RLS. Legacy cols
         (twilio_phone_number, twilio_whatsapp_number, email_from)
         retained for read-fallback so existing tenants don't lose
         data. message_kind / message_channel / message_status enums.
         message_templates table UNIQUE(tenant_id, kind, language,
         channel). message_log table with write-once trigger (only
         status / provider_id / sent_at / delivered_at / error_text
         updatable). Seed: 66 default templates for first tenant
         (11 kinds × 2 langs × 3 channels). WhatsApp content_sid =
         NULL placeholders pending Twilio+Meta per-template approval;
         body marked "[REPLACE WITH APPROVED TEMPLATE BODY]".
       - lib/twilio/{sms,whatsapp,templates}.ts. Provider helpers use
         fetch directly (no SDK). Resolve creds from settings via
         admin client. Insert message_log row 'queued' first, update
         to 'sent' or 'failed'. Silent no-op + log when creds missing
         OR NODE_ENV='test'.
       - lib/email/send.ts (Resend via fetch).
       - lib/comms/{render,dispatch}.ts. renderTemplate is mustache-
         style {{var}}, escapes HTML for email. dispatchMessage
         resolves customer.language + comm_preference, loads enabled
         template, renders, calls correct provider, writes log.
       - 3 cron routes (/api/cron/{maturity,repair-ready,layaway}-
         reminders/route.ts) gated on x-vercel-cron OR
         Bearer ${CRON_SECRET}. Idempotency via 24h dedup against
         message_log.
       - /staff/settings/communications (creds form masked once
         saved; templates grouped by kind with enable/disable toggles
         and inline editor with sample-variable preview; Send-test
         button on each).
       - SendReminderDialog wired into /staff/pawn/[id], /staff/repair/
         [id], /staff/pos/layaways/[id] detail pages (single button
         + dialog import).
       - 'export' added to AuditAction (Phase 6 used it for sends; Phase
         7 reused for report exports — audit reads share the verb).
       - Hand-rolled types in database-aliases.ts (Message* +
         SettingsCommsColumns) until 0010 applies + db:types regens.
       - Conflict resolution at merge: database-aliases.ts (Phase 5
         StripePaymentLink* vs Phase 6 Message* — both at end of
         file, disjoint additions). Union accepted.

 106. PHASE 7 SHIPPED + MERGED. Branch worktree-agent-a24d → main
     fd39163 (with 2 conflict resolutions in i18n EN/ES). Adds:
       - patches/0011-reporting.sql (NO-OP HEADER ONLY — agent
         decided no schema changes were needed for v1; aggregation is
         app-side. Header documents future cleanup: tenants.agency_
         store_id, v_loan_aging, v_inventory_turn, v_compliance_log_
         export. Apply or skip — idempotent).
       - 8 reports each with query helper + page + CSV + PDF where
         applicable: Daily Register, Pawn Aging, Loan Activity
         (Redemptions / Forfeitures / Interest), Inventory Turn,
         Sales Summary, Repair Summary, Police Report (FL Leads
         Online — CSV only), Cross-Shop Rollup (chain_hq tenants
         only via my_chain_tenant_ids() RLS helper).
       - lib/compliance/police-report/index.ts — format dispatcher
         (throws on unknown).
       - lib/compliance/police-report/formats/fl-leadsonline.ts —
         FL CSV builder. Header marked "FORMAT DRAFT — confirm with
         FL LeadsOnline before go-live". 23 CSV columns. Reads
         compliance_log ONLY (Rule 15). Marks rows
         exported_at / exported_format / exported_batch_id per batch.
         No idempotency on re-export — acceptable v1.
         store_id falls back to tenant UUID until tenants.agency_
         store_id ships.
       - lib/compliance/retention.ts — RETENTION_RULES (FL = 2y pawn /
         30d buy-hold) + canDeleteCustomer({...}) helper returning
         structured block reasons. Will hook into customer DELETE
         guard later.
       - lib/pdf/reports/ReportPDF.tsx + render-report.tsx (reuses
         existing fonts pattern).
       - components/reports/{DateRangePicker,ExportButtons,
         ReportPageShell,ReportTable}.tsx.
       - lib/format/money.ts (formatMoney/formatPercent/formatNumber/
         shortDate/shortDateTime).
       - lib/reports/{tenant-scope,api-guard,http,types}.ts shared
         infrastructure (HQ → children resolver, API guard, CSV
         writer with RFC-4180 escaping + UTF-8 BOM).
       - audit.AuditAction extended with 'export'. Every report
         download writes an audit row.
       - Reports + Compliance sidebar entries enabled (no longer
         disabled placeholders).
       - Plain HTML tables (TanStack Table v8 was specified in brief
         but agent followed existing pattern; MAX_DISPLAY=500 in DOM,
         CSV/PDF cover full set). Add tanstack-table later if you
         want virtual scroll.
       - Default eventTypes for police-report = [pawn_intake,
         buy_outright]. One-line config flip in getPoliceReportRows
         to add redemption/forfeiture if the agency requires.
       - Conflict resolutions at merge:
         * src/lib/i18n/en.ts: HEAD's last namespace was `comms`
           (added by Phase 6 merge); branch added `reports` and
           `compliance`. Conflict region encompassed both sides'
           interior of the LAST top-level namespace. Resolution:
           proper closure of comms `},` + new `reports: {...},` +
           new `compliance: {...},`. Same shape on es.ts.
         * Pattern locked: 3-way merge of additive top-level objects
           = ALWAYS UNION. Never pick one side.

 107. DASHBOARD VIBRANT-DARK PORTED INTO /staff/dashboard. Eddy
     uploaded a standalone dashboard kit (CSS + vanilla JS + HTML +
     HANDOFF.md) inline. Ported into Next 16 React: full rewrite
     of src/app/(staff)/dashboard/content.tsx; new
     src/app/(staff)/dashboard/dashboard.css (the kit's CSS scoped
     under .dashboard parent selector, :root vars hoisted to
     .dashboard, global body/* resets dropped — no leak into
     Airbnb-light surfaces); new src/app/(staff)/dashboard/actions.ts
     (searchAcrossModule server action against loans / sales /
     layaways / repair_tickets via Supabase ILIKE on ticket/sale/
     layaway numbers, tone-coded status pills per module).
     content.tsx ports the kit's vanilla-JS controller to React:
       - 5 tabs (Overview, Sales, Layaways, Pawn, Repairs).
       - Per-module signature gradients (violet/cyan/indigo/amber/
         emerald) on tabs, KPI bars, hero, action buttons.
       - Tab + range persisted to localStorage via lazy initializers
         with typeof window guards — SSR-safe AND dodges
         react-hooks/set-state-in-effect.
       - Autocomplete dropdown with ↑/↓/Enter/Escape, click-outside
         close, debounced server action invoked from onChange (NOT
         from useEffect — same lint-rule reason).
       - Recent-search chips per module persisted to localStorage
         (key shape dash.recent.{module}, max 5, deduped).
       - Existing PDF417 DlScanner reused on "Scan license"; on
         result, navigates to /customers/new (TODO: look-up-existing-
         customer-by-license branch).
       - Drill-down KPIs route via next/navigation.
       - Keyboard shortcuts: '/' focuses active search, '1'-'5'
         switch tabs.
       - Inline SVG charts (Bars / Lines / Sparkline). Demo data is
         deterministic PRNG — wire to real reports queries later.
     Visual system intentionally separate from rest of app
     (Airbnb-light remains canonical per DESIGN-airbnb.md). Strings
     hardcoded English for v1 (kit was English-only); old
     t.dashboard.* keys (customersCard, etc.) now unused but left
     in place. page.tsx unchanged — its existing real-data fetches
     (customer counts, inventory, loan/repair/sale/layaway counts,
     todayRevenue) flow into the new dashboard; KPIs needing report
     queries show '—' until Phase 7 types collapse. Commit c0dcca4.

 108. STALE PRIMARY-TREE WORK CLEANUP. Pre-session-start untracked
     dirs from a previous crashed Phase 7 attempt (?? src/app/
     (staff)/reports/, src/app/api/reports/, src/components/reports/,
     src/lib/compliance/, src/lib/pdf/reports/, src/lib/reports/)
     and their stale modifications (M Sidebar.tsx, M audit.ts,
     M en.ts/es.ts adding stale reports.* keys) were rm -rf'd /
     git checkout'd before merging the fresh Phase 7 branch. .next
     build cache cleared. Without this cleanup, git merge would
     have refused to overwrite untracked files. Lesson: when
     re-dispatching after a crash, scrub the primary tree of
     leftovers FIRST.

 109. SESSION 6 MERGE SEQUENCE. Pushed in 4 deploys:
       1. c0dcca4 — primary-tree work (bug fix + dashboard).
       2. a9b4995 — Phase 5 merge (clean, no conflicts).
       3. 0519b51 — Phase 6 merge (1 conflict in
          database-aliases.ts; union resolution).
       4. fd39163 — Phase 7 merge (2 conflicts in i18n EN+ES;
          union resolution).
     Each push triggered a Vercel deploy. Build green at 50+
     routes after final merge. tsc --noEmit clean. Lint clean
     except pre-existing use-lang.ts:29 from Phase 0. Worktrees
     and branches removed via git worktree remove -f -f +
     git branch -D after merges.

 110. CONFLICT-MERGE PATTERN LOCKED. 3-way merges of additive
     top-level objects (database-aliases.ts type aliases added at
     end of file; i18n EN/ES top-level namespaces added at end of
     object literal) ALWAYS resolve as union — keep both sides'
     additions. Conflict region typically encompasses both sides'
     contents (without their closing brace+comma); resolution =
     close the HEAD side's last block, then paste the branch
     side's blocks, close the final block. Pattern applies to
     i18n namespaces, alias type blocks, and any append-only
     structure.

================================================================================
PENDING — NEXT UP
================================================================================

  IMMEDIATE (do this first next session):
    Apply patches/0009-customer-portal.sql against Supabase. Then 0010,
    then 0011 (or skip 0011). Run npm run db:types. Collapse the manual
    hand-rolled aliases in src/types/database-aliases.ts (StripePayment
    Link* + Message* + SettingsCommsColumns) to generated forms. Drop
    the `as unknown as { from: ... }` casts the Phase 6 agent flagged
    in src/lib/twilio/*, src/lib/email/send.ts, src/lib/comms/dispatch
    .ts, the cron routes, and /staff/settings/communications. Verify
    npm run build green.

  PHASE 0 PUNCH LIST (still open):
    a. Confirm Vercel deploy status + production URL.
    b. Update NEXT_PUBLIC_APP_URL in .env.local + Vercel (Production
       env). Redeploy.
    c. Rotate the three pasted secrets (anon JWT, service_role JWT,
       sbp_ access token). Update .env.local + Vercel.
    d. Optionally revert repo-local git user.name + user.email config
       (CLAUDE.md violation — use inline `-c` overrides going forward).

  BACKLOG (in priority order):
    1. Apply 3 phase migrations + regen types + collapse aliases (above).
    2. Phase 0 close-out (deploy verify + URL update + secret rotation).
    3. Stripe Connect onboarding for first tenant. Unblocks portal
       payoff (Phase 5) AND comm cron (Phase 6) silently no-op'ing.
    4. Phase 8 — Polish:
         - Mobile audits across portal + dashboard.
         - Real Stripe smoke test once Connect onboarded.
         - Dashboard real-data wire-up (customer-name search, real KPIs
           from Phase 7 queries, real chart series, hero pawn-today /
           repairs-today computation).
         - Banner pattern rollout to /inventory/new, /pawn/new, /repair/
           new, /pos/sales/new, /inventory/transfers/new.
         - Old t.dashboard.* unused keys cleanup.
         - Dashboard string i18n migration (t.dashboard.*).
    5. WhatsApp Business per-template approval (unblocks WhatsApp
       channel — Phase 6 comms otherwise sandboxed).
    6. FL LeadsOnline format reconciliation with agency before go-live.
    7. tenants.agency_store_id ADD COLUMN migration (single column;
       police-report falls back to tenant UUID until then).
    8. DL scanner upgrades (dl_raw_payload audit column, expired-license
       guard inside createCustomerAction, front-of-license webcam).
    9. POS receipt PDF (mirrors /api/print/loan pattern).
   10. Stripe Terminal hardware reader pairing UI when readers arrive.
   11. pgsodium at-rest encryption rollout for Twilio/Resend/Stripe creds.
   12. Pre-existing use-lang.ts:29 lint error from Phase 0 — finally fix.
   13. Audit log enhancements scoped to Phase 8.
   14. Real activity feed for /dashboard (currently shows
       recentCustomers + recentItems as placeholder — wire to a true
       audit-style stream).

  KNOWN GOTCHAS TO AVOID FROM DAY 1 (carried + new):
    - Next 16 file is `proxy.ts`, exported function named `proxy`.
    - Next 16 'use server' modules can ONLY export async functions.
    - Phosphor icons crash server components — only import in 'use
      client' files.
    - Phosphor doesn't have FlaskConical — that's Lucide. Use Flask.
    - useSearchParams() must be wrapped in <Suspense> for static gen.
    - <form action={fn}> never has explicit encType.
    - Storage uploads from server actions use admin (service-role) client.
    - `npm run db:types` via the safe wrapper. Never raw `>` redirect.
    - Service role key NEVER goes in NEXT_PUBLIC_*.
    - DATABASE_URL via Supavisor pooler (not direct DB host).
    - GitHub commits MUST be authored by the identity Vercel has linked.
    - git worktree cleanup uses `-f -f` (DOUBLE-force).
    - Auth callback needs both PKCE AND implicit-flow handling.
    - Portal invites: don't use Supabase built-in SMTP.
    - profiles.role is GLOBAL only ('superadmin' | NULL).
    - user_tenants has no FK to profiles — embedded select returns null.
    - audit_log has SELECT policy only; inserts must use admin client.
    - CHECK-constrained TEXT columns come back as plain string from
      db:types — narrow at the page boundary.
    - Supabase JSONB columns need JSON-roundtrip narrowing.
    - Supabase update(patch) types reject Record<string, unknown> —
      type the patch as the table's Update alias instead.
    - First uploaded inventory photo auto-promotes to primary.
    - Storage path layout: tenant_id MUST be folder[0] for RLS to work.
    - Pawn ticket print locks core fields.
    - Forfeiture / abandonment inventory items start status='available'.
    - Cross-chain transfers blocked at trigger.
    - Use useMemo for derived state, not useEffect+setState.
    - Date.now() / random / location / etc. inside useState() trip
      the react-hooks/purity rule. Wrap in lazy initializer.
    - lib/audit.ts pulls in the admin client — never import it from a
      client component. Use lib/audit-format.ts for label helpers.
    - layaway_payments.amount allows NEGATIVE values. CHECK is `<> 0`.
    - Stripe Connect: every per-tenant call needs Stripe-Account header.
    - @fontsource fonts in @react-pdf/renderer must be loaded via
      fs.readFileSync + base64 dataURL — not require()/import.
    - SESSION 6: Setting state inside useEffect on mount triggers
      react-hooks/set-state-in-effect. Use lazy state initializers
      with typeof window guards instead. Same pattern handles SSR.
    - SESSION 6: Debounced search MUST live in onChange handler, not
      useEffect — moving setState out of effect bodies.
    - SESSION 6: When merging additive top-level objects (i18n
      namespaces, type alias blocks), ALWAYS union both sides. Never
      pick one.
    - SESSION 6: When re-dispatching an agent after a crash, scrub
      the primary tree of leftover untracked files FIRST. `git merge`
      will refuse to overwrite them otherwise.
    - SESSION 6: Validation Zod helpers — preprocess MUST handle the
      empty-string-after-trim case BEFORE the inner schema runs.
      Pattern: `(v) => typeof v === 'string' && v.trim() === '' ? null : v`.
      Trailing `.transform((v) => v ?? null)` keeps output type stable.

  KNOWN ISSUES TO REVISIT WHEN PHASES LAND:
    - Phase 5 portal: pawn ticket print PDF legal disclosure — vet
      with counsel before any live FL pawn intake.
    - Phase 5: compliance_log.customer_snapshot must include the new
      pawn fields. Verify the snapshot the intake action writes today
      still captures height/weight/marks/sex/hair/eyes/employment.
    - Phase 6: WhatsApp Content SIDs need Twilio + Meta approval per
      template per tenant before WhatsApp delivery works outside
      sandbox.
    - Phase 7: confirm current FL LeadsOnline upload format (drifts).
    - Phase 7: idempotency for compliance_log re-export (currently
      bookkeeping fields update on each export). Flag if agency
      requires strict batch-once semantics.
    - ID retention enforcement: read current FL statute right before
      portal pay-by-link ships.
    - DL scanner: dl_raw_payload TEXT column (form already POSTs the
      value; server ignores until column lands).
    - DL scanner: block-expired-licenses guard.
    - DL scanner: front-of-license photo capture.

  OUT OF SCOPE (noted for later):
    - Mobile native app.
    - Cross-tenant customer matching (blacklist-share).
    - Watch / fine-jewelry authentication services.
    - eBay / Worthy listing integration.
    - Bullion / coin spot-price feeds.
    - Wholesale buy-side (gold-buying with melt-value calculator).
================================================================================
